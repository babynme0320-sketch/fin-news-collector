# fin-news-collector 로드맵

작성: 2026-09-25 · 기준 커밋: `299c466`

## 지금 상태

| 영역 | 현재 |
| :--- | :--- |
| 뉴스 | 7개 섹션, 하루 약 80건 (한국경제 3탭·매일경제·사설·미국 뉴스) |
| 시장 | 지수·금리·환율 14종 카드 + 30년 월간 시계열(CSV 캐시) + 차트 모달 |
| 매크로 | 공포&탐욕 지수, 기준금리·CPI·실업률 |
| 리포트 | FOMC 성명서·의사록·경제전망, 하나증권 모닝브리프, KB금융 리서치 (PDF) |
| 실행 | GitHub Actions 8회/일 → `docs/index.html` 커밋 → GitHub Pages |
| 검증 | `validate_collectors.py` (0건/오류 시 워크플로 실패) — 2026-09-25 추가 |

수집·표시 계층은 이미 충분히 넓다. **다음 단계는 "더 많이 모으기"가 아니라 "모은 것을 어떻게 쓰게 할 것인가"다.**

---

## 진단: 지금 구조의 구멍 3개

### 1. 과거가 남지 않는다 (되돌릴 수 없는 손실)

- 커밋되는 산출물은 `docs/index.html` **하나뿐**이다. 8회/일 덮어쓴다.
- `reports/`는 `.gitignore` 대상이고, 보관 기간(`retention_days: 10`)이 지나면 삭제된다.
- 즉 **어제 리포트는 어디에도 없다.** 오늘 안 보면 사라진다.

수집기 코드는 언제든 고칠 수 있지만, 지나간 날의 데이터는 복구할 수 없다. 이 항목만은 "나중에"가 통하지 않는다.

### 2. 요약이 없다 (읽는 비용 > 모으는 비용)

매일 80건이 평면적으로 나열된다. 정작 알고 싶은 "오늘 중요한 게 뭔가"는 사용자가 80건을 훑어서 판단해야 한다.
수집 파이프라인은 잘 돌지만, **읽는 데 드는 시간은 자동화되어 있지 않다.**

### 3. 알려주지 않는다

리포트가 갱신되어도 사용자가 URL을 열어야 안다. 속보 섹션을 만들어놨지만 정작 속보가 떴을 때 알림이 없다.

---

## 우선순위

### Phase 1 — "아침 3분" (먼저 할 것)

#### 1-1. 아카이브 — 0.5일

- 실행 시 `docs/archive/report_YYYYMMDD.html`로도 커밋하고, `docs/archive/index.html`에 날짜 목록을 만든다.
- GitHub Pages에 영구 보관된다(Actions 캐시·`reports/`와 달리 사라지지 않음).
- 워크플로 변경은 복사 한 줄 추가 수준. 데이터 손실이 없는 작업이라 **가장 먼저** 해야 한다.

#### 1-2. 오늘의 요약 — 1일

- 헤드라인 80건 → **핵심 5줄 + 주목 키워드**. 리포트 최상단 "오늘의 핵심" 카드.
- 재료는 이미 다 있다: 섹션별 제목·날짜·요약(lede).
- 실행 위치에 따라 두 갈래:
  - **로컬(launchd)**: 이미 쓰고 있는 `~/bin/deepseek` CLI 재사용 → API 키 관리 불필요.
  - **CI**: GitHub Secrets에 키를 넣고 `run.py`에서 호출. 대신 키 노출·비용 관리가 생긴다.
- 주의: 요약이 실패해도 리포트는 정상 생성되어야 한다(이 프로젝트의 기존 실패 격리 원칙과 동일).

#### 1-3. 알림 — 0.5일

- 텔레그램 봇. 아침 1회 요약 5줄 + 링크. 속보 감지 시 즉시(중복 방지 스로틀).
- `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` 두 개만 Secrets에 넣으면 된다.
- 이게 붙으면 "8회/일 갱신"이 처음으로 의미를 갖는다. 지금은 갱신해도 아무도 모른다.

> Phase 1의 셋은 서로를 강화한다. 아카이브가 있어야 어제와 비교할 수 있고, 요약이 있어야 알림이 짧아지고, 알림이 있어야 요약을 읽는다.

---

### Phase 2 — 노이즈 줄이기

#### 2-1. 이슈 클러스터링 — 검토 후 **보류** (2026-09-25)

가설: 같은 사안을 여러 매체가 다루니 제목 유사도로 묶어 대표 1건만 노출하자.

**측정 결과 가설이 틀렸다.** 지난 3일치 리포트(88·33·46건)에서 교차 매체 제목 유사도를
전수 계산했더니 최대 0.25였고, 임계값 0.4~0.7 어디서도 교차 매체 클러스터가 **0개**였다.
매체별 주제가 이미 분리돼 있어(한국경제 산업·경제 / 매일경제 경제 / 미국 뉴스 / 사설)
중복이 발생하지 않는다.

그래서 구현(`clustering.py`)과 테스트는 지웠다. 동작하지 않는 기능을 남기면
비어 있는 섹션과 관리 비용만 생긴다. 재검토 조건: 매체를 늘려 주제가 겹치기 시작하거나
(예: 매일경제 증권 섹션 추가), 같은 소스 안에서 같은 기사가 여러 탭에 중복될 때.
`textutil.content_tokens()`는 요약 근거 검증에 쓰이므로 남겨뒀다.

#### 2-2. 관심 키워드 — 0.5일

- `sources.yaml`에 `keywords:` 목록을 두고, 리포트에서 하이라이트 + 상단 별도 섹션.
- 개인화의 최소 버전. 별도 계정·DB 없이 YAML 한 줄로 끝난다.

#### 2-3. 읽음 표시 — 선택

- 관심 기사를 클릭하면 `localStorage`에 기록해 다음 방문 때 흐리게. 서버가 필요 없다.

---

### Phase 3 — 데이터 확장

| 항목 | 내용 | 상태 |
| :--- | :--- | :--- |
| 국고채 복구 | 네이버 소스가 죽어 **FRED(한국 장기금리)로 교체** — 2026-08까지 갱신 | **완료** |
| 경제지표 확대 | 미국 기준금리·근원 CPI·비농업 고용 추가(FRED, 키 불필요) | **완료** |
| 증권사 리포트 확대 | 미래에셋 추가 (EUC-KR + JS 래핑 링크 → `encoding`·`link_pattern`) | **완료** |
| DART 공시 | 주요사항보고서·실적 공시. 무료 API 키 필요 | 대기(키) |
| 한국 CPI·산업생산 | FRED의 한국 시리즈는 2023~24년에서 멈춰 있어 못 씀 | 막힘(키) |

#### 국고채: 소스가 죽어 있었다 (2026-09-25 확인 → FRED로 복구)

`국고채 10년`을 추가하려다 **3년물조차 이미 죽어 있는 것을 발견**했다.
네이버 marketindex가 `stock.naver.com`으로 이전하면서 `interestDailyQuote.naver`가 **410 Gone**을
반환한다. 새 페이지는 JS로만 렌더돼 정적 요청으로는 값을 못 가져온다.
다음 금융·KRX·삼성증권도 봇 요청을 막는다(403/410).

그동안 리포트에는 캐시된 마지막 값이 **현재값처럼** 표시되고 있었다(다른 지표는 360개월치인데
국고채만 15개월치, 2026-09-17에서 멈춤). FRED `IRLTLT01KRM156N`으로 교체해 4.29%(2026-08)가
들어온다. 일간이 아니라 **월간**이라 카드 이름에 명시했다.

**막힌 지점이 하나 더 있었다**: FRED는 TLS 지문으로 파이썬 HTTP 클라이언트를 거부한다.
같은 CI 러너에서 curl은 200을 0.038초에 받는데 requests는 ReadTimeout이었다(OpenSSL 3.x인
CI에서도 동일 → LibreSSL 문제가 아님). `collectors/fred.py`는 requests 실패 시 curl로 폴백한다.

**남은 것**: 한국 CPI·산업생산은 FRED 시리즈가 2023~24년에서 멈춰 있어 대안이 없다.
한국은행 ECOS API 키(무료)를 받으면 해결된다.

---

### Phase 4 — 운영 안정성

#### 4-1. Actions 캐시 키 정리 — 0.5일

현재 캐시 키가 `github.run_id`를 포함해 **매 실행마다 새 캐시 엔트리가 쌓인다**(현재 30개 / 55MB, `sokbo-*`가 실행 1회당 1개씩 증가 중). 내용이 같아도 새로 저장하므로 재사용이 안 된다.

- 안정 키 + 내용 해시(`hashFiles`)로 바꾸면 변경이 있을 때만 새 엔트리가 생긴다.
- 당장 장애는 아니지만 방치하면 저장소 캐시 한도(10GB)까지 계속 자란다.

#### 4-2. 수집량 감소 감지 — 0.5일

`validate_collectors.py`는 **0건**을 잡는다. 그런데 사이트 개편으로 "평소 10건 → 3건"이 되는 조용한 열화는 못 잡는다. 직전 실행 대비 급감(예: -50%) 시 경고를 추가한다.

#### 4-3. 실행 빈도 조정 — 0.5일

8회/일 실행되는데 대부분의 실행에서 내용이 거의 같다. 장 시작·장 마감·아침 브리핑 3회로 줄이고, 요약(1-2)은 하루 1회만 생성하면 비용과 노이즈가 함께 줄어든다.

---

## 하지 않는 것 (지금은)

| 후보 | 하지 않는 이유 |
| :--- | :--- |
| 로그인·다중 사용자 | 1인용 도구. 사용자 개념이 생기는 순간 복잡도가 배로 늘어난다 |
| 데이터베이스 | CSV + git 커밋으로 충분하다. 부분 실패에 강한 현재 구조를 유지 |
| 실시간 스트리밍 | 금융 뉴스 열람은 8회/일 갱신으로 충분. 실시간은 읽는 사람을 지치게 한다 |
| 모바일 앱 | GitHub Pages + 반응형 레이아웃이 이미 동작한다. PWA 매니페스트로 충분 |

---

## 기존 계획서 처리

`market_dashboard_expansion_plan.md`(14종 지표 + 30년 대시보드 + 모닝루틴 연동)는 **대부분 반영됐다.**

- 완료: 14종 지표, 30년 시계열 CSV 증분 캐시, 월간 다운샘플링, 차트 모달 + 기간 필터, 그리드 카드 UI
- 미반영: 모닝루틴(`/mr`) 연동 — 현재 한국경제 소스는 `/industry`·`/economy`·`/opinion`을 쓴다. 모닝루틴 방송 연동을 다시 원하면 `sources.yaml`에 소스를 하나 더 추가하면 된다(코드 수정 불필요)
- 참고: `/mr` 페이지는 살아 있고 수집도 된다. 다만 최신 기사 목록이 아니라 방송 편성 페이지에 가까워, 뉴스 소스로는 현재 구성이 더 낫다고 판단해 제외했다

이 문서는 `docs/archive/`(또는 리포지토리 루트의 `archive/`)로 옮겨 기록으로 남기는 것을 권한다.

---

## 진행 상황 (2026-09-25)

Phase 1·2·4와 Phase 3의 상당 부분을 구현하고 CI에서 검증해 머지했다.

| 단계 | 항목 | 상태 |
| :--- | :--- | :--- |
| 1-1 | 아카이브 | 완료 |
| 1-2 | 오늘의 핵심 요약(근거 검증 포함) | 완료 — CI 키만 넣으면 켜짐 |
| 1-3 | 텔레그램 알림 | 완료 — 토큰만 넣으면 켜짐 |
| 2-1 | 이슈 클러스터링 | **보류** (실측 결과 중복 0건) |
| 2-2 | 관심 키워드 | 완료 |
| 3-1 | DART 공시 | 대기 — API 키 필요 |
| 3-2 | 미래에셋 리서치 | 완료 |
| 3-3 | 경제지표 확대 + 국고채 복구 | 완료 |
| 4-1 | 캐시 키 정리 | 완료 |
| 4-2 | 수집량 급감 감지 | 완료 |
| 4-3 | 실행 빈도 조정 | **철회** — 속보 알림이 생겨 자주 도는 게 이득 |

### 사용자 키가 필요한 것 (없으면 조용히 건너뜀)

| 기능 | 필요한 것 | 얻는 것 |
| :--- | :--- | :--- |
| 요약을 CI에서도 | GitHub Secret `DEEPSEEK_API_KEY` | 리포트 상단 5줄 요약 |
| 알림 | `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` + `notify.enabled: true` | 아침 브리핑 + 속보 알림 |
| DART 공시 | opendart API 키 | 실적·주요사항 공시 |
| 한국 CPI·산업생산 | 한국은행 ECOS 키 | 국내 물가·생산 지표 |

### 다음에 검토할 것

- **매일경제 증권 섹션 추가** — 지금 매체별 주제가 분리돼 중복이 없다. 주제가 겹치기
  시작하면 그때 클러스터링(2-1)을 다시 본다.
- **요약을 하루 1회로** — 지금은 8회 실행마다 새로 만든다. 비용은 미미하지만
  하루 종일 요약이 바뀌는 게 좋은지 아닌지는 취향 문제다.
- **읽음 표시** — 클릭한 기사를 `localStorage`에 기록. 서버가 필요 없다.
